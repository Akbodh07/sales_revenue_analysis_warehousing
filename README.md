# sales_revenue_analysis_warehousing
This project builds an end-to-end analytics pipeline for e-commerce sales data. Starting from raw CSVs (Bronze layer), the data is staged in SQL Server, cleaned and transformed (Silver layer), and modeled into a star schema (Gold layer).
